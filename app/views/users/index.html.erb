<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate @users, previous_label: " &lt 前へ", next_label: "次へ &gt" %>

<ul class="users">
  <%= render @users %>
</ul>

<%= will_paginate @users, previous_label: " &lt 前へ", next_label: "次へ &gt" %>
